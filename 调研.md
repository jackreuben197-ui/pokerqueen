# pokerqueen

## 项目目录

```
assets/                 # 静态资源目录
    ├── bundles/            # 资源包
    │     ├──texas              # 包含德州玩法的游戏场景和界面、UI元素预制和场景
    ├── effect/             # 特效资源
    ├── resources/          # 包含配置和多语言文件、登录和主界面的预制和场景
    ├── scene/              # 场景文件 主场景
    ├── script/             # 脚本文件 
    ├── sound/              # 音效资源
    ├── temp/               # 临时文件 一些ui设计元素
    └── texture/            # 纹理资源 ui纹理
```

## 代码目录

```
script/                 # 脚本文件目录
    ├── common/             # 通用代码 包含UI点击、列表实现、UI界面基类、单例基类等
    ├── config/             # 配置文件 区号定义、枚举定义、GanmeConfig、事件消息定义等
    ├── define/             # 通用定义 颜色、枚举定义
    │     ├── UIDefine.ts       #UI目录定义脚本 
    ├── dotween/            
    ├── event/              # 事件系统
    ├── frame/              # 框架
    │     ├── base/             #UI组件类和数据类的基类目录
    │     ├── data/             #http协议的数据结构
    │     ├── manager/          #声音、数据、多语言、本地化等管理单例实现
    ├── funcomponent/       #
    ├── game/               # 游戏内逻辑
    ├── gm/                 # 
    ├── helper/             # 字符串、时间、头像加载等的处理工具类
    ├── lobby/              # 大厅逻辑
    ├── manager/            # 流程管理、资源管理、toast管理等
    ├── mtt/                # mtt面板逻辑目录
    ├── net/                # https/websocket底层实现逻辑   
    ├── procedure/          # 游戏大的流程fsm管理类    
    ├── protobuf/           # socket协议
    ├── session/            #    
    ├── sound/              # 
    ├── statemachine/       #    
    ├── tools/              # 
    ├── ui/                 # 
    ├── wallet/             # 
```

## UI添加流程

以朋友桌邀请界面为例

```
    1.在resource/main/lobby/prefab下创建界面预制UICreateFriendMatchHome.prefab
    2.在script/lobby/下添加ts代码UICreateFriendMatchHome.ts
    3.添加的UI代码继承UIBase或者BaseForm、修改好分组(方便添加到预制上)
        UIBase和BaseForm的区别
        1.像主界面这些无需打开关闭动画的继承自UIBase,否则BaseForm
        2.BaseForm本身继承于UIBase,实现了一些打开关闭界面需要的生命周期方法
    4.将代码当成组件添加到预制上
    5.调用逻辑
        LobbyControl.getInstance().switchContent("UICreateFriendMatchHome")
        UIComponent.open(UIDefine.UICreateMatch, null, { SceneUI: SceneManager.Instance.currUI });    
    6.点击事件也是通过拖拽代码里的方法添加上去的
```


## protobuf更新

1.协议更新地址
```
https://gitlab.com/darkalliance/agreement-web
```

2.确保js文件和d.ts文件都复制过来,d.ts类似于c的变量声明作用

3.手动更新ProtocolHoldemMessages.ts对应的代码

## http和websocket连接更新

1.GameConfig.ts中配置了服务器域名

2.ProcedureConfig中拼接的websocket连接的详细地址
```
    //老的
    wss://${GameConfig.Web_Host_Test1}/api/channel/

    //新的
    ws://${GameConfig.Web_Host_Test1}{0}
```

## 打包部署

1.cocos create项目内点击"项目/构建发布"
2.复制构建好的文件夹到cocos_release仓库
```
https://gitlab.com/darkalliance/client/cocos/cocos_release
```
3.提交到dev或者test分支
4.提交请求合并到main分支
5.然后需要孔发布(这里是需要验证gitlab才能部署，但是验证需要海外的手机号)
````

