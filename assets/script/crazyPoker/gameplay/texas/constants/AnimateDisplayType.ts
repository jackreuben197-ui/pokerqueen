export enum AnimateDisplayTypeCards {
    Static = 0, // 纯静态变化
    Deal = 1, // 发牌
    ShowCards = 2 // 展示牌
}

export enum AnimateDisplayTypeRoundBet {
    Static = 0, // 纯静态变化
    PutNear = 1 // 近距离进一个池
}

export enum AnimateDisplayTypeButton {
    Static = 0, // 纯静态变化
    Next = 1 // 从上一个过来
}

export enum AnimateDisplayTypeWin {
    Static = 0,
    Play = 1 // 展示动画
}

export enum AnimateDisplayTypePublicCards {
    Static = 0, // 纯静态变化
    Deal = 1 // 发牌
}

export enum AnimateDisplayTypePosition {
    Static = 0, // 纯静态变化
    ToTarget = 1
}

export enum AnimateDisplayTypeAction {
    Static = 0, // 纯静态变化
    ShowAction = 1
}
